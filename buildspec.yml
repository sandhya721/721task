version: 0.2

phases:
  install:
    commands:
      - echo Logging in to Amazon ECR...

  pre_build:
    commands:
      - echo Pre build phase

  build:
    commands:
      - echo Build started
      - docker build -t my-app .

  post_build:
    commands:
      - echo Build completed
